{
  "description": "Nudge toward gh-security resolve-alerts when a Bash command's output carries a GitHub vulnerability notice or package manager audit findings.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/common/notice-scan.sh",
            "args": []
          }
        ]
      }
    ]
  }
}
