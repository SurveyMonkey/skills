---
paths:
  - "spec/**"
---

# Specs

This repository's test strategy is the `testing` skill: invoke it with the Skill tool (`testing`, or `/testing`) before writing or reviewing an example here.

It owns seams, asserting the verdict rather than the parse, where an expected value may come from, red-first, what may be mocked, when a prose pin is legitimate, and the review checklist. The gate commands and the ShellCheck rules stay in the root `CLAUDE.md`.
