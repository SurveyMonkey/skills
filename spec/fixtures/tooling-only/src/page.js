// Nothing here imports the package under test or its parents: the pin this
// fixture stands for is reached only by the build tooling (the tooling-only
// field-test case).
export const title = 'home';
