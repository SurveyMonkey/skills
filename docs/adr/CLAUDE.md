# ADRs

This directory holds all Architecture Decision Records (convention:
`.claude/rules/path-docs-adr.md`), one file per ADR named `NNN-descriptive-slug.md`
(Status / Context / Decision / Consequences).
